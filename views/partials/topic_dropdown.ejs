<select name="topic" id="topic_dropdown" type="dropdown">
  <option value="" selected disabled>Select Topic</option>
  <% for (const topic of topics) { %>
    <%console.log(topic)%>
  <option value="<%= topic.id %> "><%= topic.name %></option>
  <% } %>
</select>
